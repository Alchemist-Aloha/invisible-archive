name: invisible_archive_mobile
description: A new Flutter project for Invisible Archive.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.5
  video_player: ^2.7.0
  photo_view: ^0.14.0
  intl: ^0.18.1
  path: ^1.8.3
  url_launcher: ^6.1.12
  cached_network_image: ^3.2.3
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
